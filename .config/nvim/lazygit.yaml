os:
  edit: 'nvim --server "$NVIM_SERVER" --remote-send "<C-g>:lua require(\"utils\").edit_file(\"{{filename}}\")<CR>"'
  editAtLine: 'nvim --server "$NVIM_SERVER" --remote-send "<C-g>:lua require(\"utils\").edit_file(\"{{filename}}\", {{line}})<CR>"'
